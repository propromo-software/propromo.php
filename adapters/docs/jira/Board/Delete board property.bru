meta {
  name: Delete board property
  type: http
  seq: 20
}

delete {
  url: {{host}}/board/{boardId}/properties/{propertyKey}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{JIRA_API_TOKEN}}
}
