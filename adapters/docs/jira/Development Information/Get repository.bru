meta {
  name: Get repository
  type: http
  seq: 2
}

get {
  url: {{host-4}}/repository/{repositoryId}
  body: none
  auth: bearer
}

headers {
  Authorization: 
}

auth:bearer {
  token: {{JIRA_API_TOKEN}}
}
