meta {
  name: Create reaction for a release
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/repos/:owner/:repo/releases/:release_id/reactions
  body: json
  auth: none
}

headers {
  Content-Type: application/json
  Accept: application/json
}

body:json {
  {
    "content": "hooray"
  }
}
