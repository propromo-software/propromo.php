meta {
  name: List repository security advisories
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/repos/:owner/:repo/security-advisories?direction=desc&sort=created&before=<string>&after=<string>&per_page=30&state=published
  body: none
  auth: none
}

query {
  direction: desc
  sort: created
  before: <string>
  after: <string>
  per_page: 30
  state: published
}

headers {
  Accept: application/json
}
