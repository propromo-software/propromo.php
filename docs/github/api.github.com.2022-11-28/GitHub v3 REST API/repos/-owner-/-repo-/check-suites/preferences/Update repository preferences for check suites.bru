meta {
  name: Update repository preferences for check suites
  type: http
  seq: 1
}

patch {
  url: {{baseUrl}}/repos/:owner/:repo/check-suites/preferences
  body: json
  auth: none
}

headers {
  Content-Type: application/json
  Accept: application/json
}

body:json {
  {
    "auto_trigger_checks": [
      {
        "app_id": "<integer>",
        "setting": true
      },
      {
        "app_id": "<integer>",
        "setting": true
      }
    ]
  }
}
