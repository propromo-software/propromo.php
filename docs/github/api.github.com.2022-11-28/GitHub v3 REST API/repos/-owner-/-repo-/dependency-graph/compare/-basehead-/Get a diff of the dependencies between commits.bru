meta {
  name: Get a diff of the dependencies between commits
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/repos/:owner/:repo/dependency-graph/compare/:basehead?name=<string>
  body: none
  auth: none
}

query {
  name: <string>
}

headers {
  Accept: application/json
}
