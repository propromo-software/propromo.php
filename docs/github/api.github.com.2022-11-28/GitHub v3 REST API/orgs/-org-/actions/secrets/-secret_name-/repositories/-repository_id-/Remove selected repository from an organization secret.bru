meta {
  name: Remove selected repository from an organization secret
  type: http
  seq: 2
}

delete {
  url: {{baseUrl}}/orgs/:org/actions/secrets/:secret_name/repositories/:repository_id
  body: none
  auth: none
}
