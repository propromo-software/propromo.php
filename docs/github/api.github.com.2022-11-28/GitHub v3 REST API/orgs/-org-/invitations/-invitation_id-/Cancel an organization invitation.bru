meta {
  name: Cancel an organization invitation
  type: http
  seq: 1
}

delete {
  url: {{baseUrl}}/orgs/:org/invitations/:invitation_id
  body: none
  auth: none
}

headers {
  Accept: application/json
}
