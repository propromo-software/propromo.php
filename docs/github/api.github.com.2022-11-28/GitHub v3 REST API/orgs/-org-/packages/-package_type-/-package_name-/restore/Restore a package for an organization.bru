meta {
  name: Restore a package for an organization
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/orgs/:org/packages/:package_type/:package_name/restore?token=<string>
  body: none
  auth: none
}

query {
  token: <string>
}

headers {
  Accept: application/json
}
