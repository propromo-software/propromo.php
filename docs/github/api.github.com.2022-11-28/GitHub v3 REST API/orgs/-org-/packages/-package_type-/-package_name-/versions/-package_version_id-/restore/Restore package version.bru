meta {
  name: Restore package version for an organization
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/orgs/:org/packages/:package_type/:package_name/versions/:package_version_id/restore
  body: none
  auth: none
}

headers {
  Accept: application/json
}
