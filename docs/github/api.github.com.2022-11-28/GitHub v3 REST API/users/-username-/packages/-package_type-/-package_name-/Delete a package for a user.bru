meta {
  name: Delete a package for a user
  type: http
  seq: 2
}

delete {
  url: {{baseUrl}}/users/:username/packages/:package_type/:package_name
  body: none
  auth: none
}

headers {
  Accept: application/json
}
