meta {
  name: Restore a package for a user
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/users/:username/packages/:package_type/:package_name/restore?token=<string>
  body: none
  auth: none
}

query {
  token: <string>
}

headers {
  Accept: application/json
}
