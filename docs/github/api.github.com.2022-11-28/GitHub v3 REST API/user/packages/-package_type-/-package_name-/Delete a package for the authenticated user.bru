meta {
  name: Delete a package for the authenticated user
  type: http
  seq: 2
}

delete {
  url: {{baseUrl}}/user/packages/:package_type/:package_name
  body: none
  auth: none
}

headers {
  Accept: application/json
}
