meta {
  name: Delete a public SSH key for the authenticated user
  type: http
  seq: 2
}

delete {
  url: {{baseUrl}}/user/keys/:key_id
  body: none
  auth: none
}

headers {
  Accept: application/json
}
