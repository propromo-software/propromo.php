meta {
  name: Add a selected repository to a user secret
  type: http
  seq: 1
}

put {
  url: {{baseUrl}}/user/codespaces/secrets/:secret_name/repositories/:repository_id
  body: none
  auth: none
}

headers {
  Accept: application/json
}
