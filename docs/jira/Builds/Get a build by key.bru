meta {
  name: Get a build by key
  type: http
  seq: 3
}

get {
  url: {{host-2}}/pipelines/{pipelineId}/builds/{buildNumber}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{JIRA_API_TOKEN}}
}
