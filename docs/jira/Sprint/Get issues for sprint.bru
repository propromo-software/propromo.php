meta {
  name: Get issues for sprint
  type: http
  seq: 6
}

get {
  url: {{host}}/sprint/{sprintId}/issue
  body: none
  auth: bearer
}

query {
  ~startAt: 
  ~maxResults: 
  ~jql: 
  ~validateQuery: 
  ~fields: 
  ~expand: 
}

auth:bearer {
  token: {{JIRA_API_TOKEN}}
}
