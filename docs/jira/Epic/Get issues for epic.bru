meta {
  name: Get issues for epic
  type: http
  seq: 5
}

get {
  url: {{host}}/epic/{epicIdOrKey}/issue
  body: none
  auth: bearer
}

query {
  ~startAt: 
  ~maxResults: 
  ~jql: 
  ~validateQuery: 
  ~fields: 
  ~expand: 
}

auth:bearer {
  token: {{JIRA_API_TOKEN}}
}
