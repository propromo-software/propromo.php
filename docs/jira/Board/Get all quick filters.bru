meta {
  name: Get all quick filters
  type: http
  seq: 21
}

get {
  url: {{host}}/board/{boardId}/quickfilter
  body: none
  auth: bearer
}

query {
  ~startAt: 
  ~maxResults: 
}

auth:bearer {
  token: {{JIRA_API_TOKEN}}
}
